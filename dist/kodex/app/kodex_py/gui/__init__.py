"""Kodex GUI — tkinter-based interface for Windows.

All GUI modules use tkinter (bundled with Python on Windows).
On systems without tkinter (headless Linux), import will fail gracefully
and the app falls back to CLI/tray-only mode.
"""
