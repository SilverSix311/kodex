"""Kodex GUI — CustomTkinter-based interface for Windows.

All GUI modules use CustomTkinter for modern, native-looking UI.
On systems without customtkinter, import will fail gracefully
and the app falls back to CLI/tray-only mode.
"""
